name = "ask-gustavo"
main = "src/index.js"
compatibility_date = "2024-10-01"

# Non-secret config. Set to your site origin to lock down CORS.
[vars]
ALLOWED_ORIGIN = "https://guhcostan.github.io"

# Secret (DO NOT put the key here). Set it with:
#   wrangler secret put GROQ_API_KEY
# or let the GitHub Actions workflow set it from the GROQ_API_KEY repo secret.
